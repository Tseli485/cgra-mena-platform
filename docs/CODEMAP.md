# Codemap — pwa/index.html (sections, ordre réel)
CSS design system « dossier officiel » (~l.10-280) → HTML vues (dashboard/dossiers/lifecycle/legislation/conseil/cases/guide/resources) → modals (dossier, fiche, detail, backup, tr, gloss, tour, arch, calc, cgra, stats, wn, ana) → JS : I18N+setLang → crypto (deriveKey/aes*/setup/unlock/persist) → CRUD dossiers/checklist → dashboard (sélecteur échéances + matrice) → renderers data → justices de paix → RAPPORTS_TYPE+rtFind → fiche import PDF (parseFicheText/PDF_IMPORT) → NL_FR (js/nl-fr-dict.js) + trDocNL/FR + openTrad → guide/intervenants/banques → rdv/ics → frais (€/km) → archives (mena_archive_enc) → ficheView (rubriques) → pj IndexedDB chiffrées → notifications → formatDocPro → bergamot (BG_DIR_PAIRS nlfr/frnl) → analyse DOC_DETECTORS (19) + conseils/journal → alerte juridique → DELAI_EVENTS/computeDelais → CGRA_Q prep → stats → CHANGELOG/whatsNew → utils (esc/addDays LOCAL).
Stores: localStorage mena_auth, mena_dossiers_enc, mena_archive_enc, mena_lang/theme/pkm/seen_v/alert_seen/tour/notif/last_export ; IndexedDB mena_pj ; Cache bergamot-v1.
